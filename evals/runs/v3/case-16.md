**Commit 16 (8a39d5e): Skipped — no release note.**

This is an eval/project-meta change. Moving run-01 results into the docs directory so they publish to the site affects the project's own published materials, not anyone using the book-recommendation skill. Per the skip rule, publishing eval results to a site doesn't count as user-visible, so no entry is generated for it.

Note: I wasn't able to verify this commit against the actual repository, since no repo location is available in context. The above is based on the description you provided.
