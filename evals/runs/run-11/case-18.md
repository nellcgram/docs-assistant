No release note for this one.

Skipped: commit 18 — the change was to an example/sample data file, which doesn't alter what a user of the book-recommendation skill sees. (Not verifiable against the repo, since it isn't available in context; this is based on the description as given.)

If that file is actually the live already-read list the recommender checks against rather than a sample, tell me and I'll write the entry, since fixing it would change which books get excluded.
