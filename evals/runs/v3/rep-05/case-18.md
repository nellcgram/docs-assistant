Commit 18 (b50af03): Corrected the example list of books you had already read, so the recommender read it properly and left those titles out of its suggestions.

Note: I could not verify this commit against the repository, so the entry is based only on the description given. The description does not say what was wrong with the example, so the release note cannot name the specific defect — if you can tell me what the fix corrected (for example, a formatting problem or a wrong entry), I can make the note specific.
